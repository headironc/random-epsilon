services:
    random-epsilon:
        container_name: random-epsilon
        image: random-epsilon:latest
        restart: on-failure:3
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 3000:3000
        networks:
            - nginx

networks:
    nginx:
        external: true
        name: nginx
